/**
 * Hook que carrega automaticamente a última clínica selecionada
 * quando o aplicativo é iniciado
 * 
 * @deprecated Este hook não é mais necessário no sistema de documentação.
 * A seleção de clínica é gerenciada pelo useAuthStore.
 */
export function useAutoLoadClinic() {
  // Hook vazio - mantido para compatibilidade mas não faz nada
  return null
}
